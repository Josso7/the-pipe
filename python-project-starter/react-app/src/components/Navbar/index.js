import { NavLink } from 'react-router-dom'

function Navbar(){
    return (
        <div>
            <Link exact to='/'>
                
            </Link>
        </div>
    )
}

export default Navbar;
